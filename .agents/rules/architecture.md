---
trigger: always_on
---

Streaming Requirement: "All LLM responses must be implemented using Server-Sent Events (SSE) for a real-time typing effect in the UI."

Auth Guard: "Every API route and worker task must verify the user_id from Clerk before performing operations."

Clean Code: "Prefer functional patterns in TypeScript and Type Hints in Python. No any types allowed."